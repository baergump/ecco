package at.jku.isse.ecco.adapter.c.test;

public class CReaderTest {

    // test with files:

    // empty program (zero codeLines) (no FTs)
    // single line (no FTs)
    // multiple lines (no FTs)
    // empty function (no FTs)
    // function with single line (no FTs)
    // function with multiple line (no FTs)
    // complex function (no FTs)
    // lines before function
    // lines after function
    // multiple functions without lines
    // multiple functions with lines in between

    // line with FT
    // function with FT

    // multiple of these files
}
